[pytest]
testpaths = tests
asyncio_mode = auto

markers =
    unit: mark a test as a unit test (fast, no external services)
    integration: mark a test as an integration test (may use mocked external services)
    slow: mark a test as slow-running
